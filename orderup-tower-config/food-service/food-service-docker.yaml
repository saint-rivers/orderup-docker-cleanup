server:
  port: 5689


spring:
  application:
    name: food-service
  datasource:
    url: jdbc:postgresql://food-database:5432/food_db
    username: useradmin
    password: useradmsecret

  jpa:
    hibernate:
      ddl-auto: update
      dialect=org:
        hibernate:
          dialect:
            PostgreSQL9Dialect:show-sql: true